@font-face {
  font-family: "Long";
  src: url("/fonts/long.woff");
}

@font-face {
  font-family: "Round";
  src: url("/fonts/round.woff");
}

@font-face {
  font-family: "Round Bold";
  font-weight: bold;
  src: url("/fonts/round-bold.woff");
}

body {
  width: 100vw;
  overflow-x: hidden;
  font-family: "Round", sans-serif;
  background-color: black;
  margin: 0;
  padding: 0;
}

main {
  width: 100vw;
  overflow-x: hidden;
  background: linear-gradient(
    223.17deg,
    rgb(28, 24, 41) 0%,
    rgb(27, 24, 40) 8.61%,
    rgb(25, 23, 36) 17.21%,
    rgb(22, 21, 32) 25.82%,
    rgb(20, 19, 28) 34.42%,
    rgb(18, 18, 24) 43.03%,
    rgb(17, 17, 23) 51.63%
  );
}

/* Utility Classes */

.flex-center {
  display: flex;
  justify-content: center;
  align-items: center;
}

.col-center {
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
}

.abs-center {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
}

/* Components */

nav {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 1.25rem; /* default for p-5 */
  z-index: 100;
}

@media (min-width: 768px) {
  nav {
    padding: 4rem; /* md:p-16 */
  }
}

.gradient-title {
  font-family: "Round Bold", sans-serif;
  font-weight: 800; /* !font-extrabold */
  text-transform: uppercase;
  text-align: center;
  background: linear-gradient(to right, #f472b6, #f87171, #fb923c);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  font-size: 5rem;
  line-height: 4.5rem;
}

@media (min-width: 768px) {
  .gradient-title {
    font-size: 8rem;
    line-height: 7rem;
  }
}

.hero-section {
  width: 100vw;
  height: 100vh;
  position: relative;
  overflow: hidden;
}

.hero-section .scale-out {
  object-fit: cover;
  height: 100%;
}

@media (min-width: 768px) {
  .hero-section .scale-out {
    transform: scale(1.25);
  }
}

.hero-section .title-logo {
  position: absolute;
  top: 0;
  height: 100%;
  object-fit: cover;
}

@media (min-width: 768px) {
  .hero-section .title-logo {
    transform: scale(1.25);
  }
}

.hero-section .trailer-logo {
  position: absolute;
  bottom: -1.25rem; /* -bottom-5 */
  left: 50%;
  transform: translateX(-50%);
  width: 12rem; /* w-48 */
}

.hero-section .play-img {
  border-radius: 9999px;
  background: white;
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  display: flex;
  justify-content: center;
  align-items: center;
  width: 5rem; /* size-20 */
  height: 5rem;
}

@media (min-width: 768px) {
  .hero-section .play-img {
    width: 7rem; /* size-28 */
    height: 7rem;
  }
}

.hero-section .fake-logo-wrapper {
  position: absolute;
  z-index: 50;
  left: 50%;
  transform: translateX(-50%);
  top: 8.5rem; /* default md */
}

@media (max-width: 639px) {
  .hero-section .fake-logo-wrapper {
    top: 9.5rem; /* xs */
  }
}

@media (min-width: 640px) and (max-width: 767px) {
  .hero-section .fake-logo-wrapper {
    top: 12.8rem; /* sm */
  }
}

@media (min-width: 1536px) {
  .hero-section .fake-logo-wrapper {
    top: 11rem; /* 2xl */
  }
}

@media (min-width: 1920px) {
  .hero-section .fake-logo-wrapper {
    top: 12rem; /* 3xl */
  }
}

.hero-section .overlay-logo {
  width: 15rem; /* default w-60 */
  height: auto;
  object-fit: cover;
  opacity: 0;
}

@media (min-width: 1536px) {
  .hero-section .overlay-logo {
    width: 18rem; /* 2xl:w-72 */
  }
}

@media (min-width: 1920px) {
  .hero-section .overlay-logo {
    width: 20rem; /* 3xl:w-80 */
  }
}

.first-vd-wrapper .first-vd {
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: 75% center;
}

@media (min-width: 768px) {
  .first-vd-wrapper .first-vd {
    object-position: 50% center;
  }
}

.entrance-message {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100vh;
  overflow: hidden;
  z-index: 20;
  opacity: 1;
}

.entrance-message .entrance-logo {
  position: absolute;
  left: 50%;
  transform: translateX(-50%);
  width: 12rem; /* w-48 default */
  top: 8.5rem;
}

@media (max-width: 639px) {
  .entrance-message .entrance-logo {
    top: 9.5rem; /* xs */
  }
}

@media (min-width: 640px) and (max-width: 767px) {
  .entrance-message .entrance-logo {
    top: 12.8rem; /* sm */
  }
}

@media (min-width: 768px) {
  .entrance-message .entrance-logo {
    width: 15rem; /* md:w-60 */
  }
}

@media (min-width: 1536px) {
  .entrance-message .entrance-logo {
    width: 18rem; /* 2xl:w-72 */
    top: 11rem;
  }
}

@media (min-width: 1920px) {
  .entrance-message .entrance-logo {
    width: 20rem; /* 3xl:w-80 */
    top: 12rem;
  }
}

/* === BLACK GRADIENT BG === */

.black-gradient-bg {
  background: linear-gradient(
    223.17deg,
    rgb(28, 24, 41) 0%,
    rgb(27, 24, 40) 8.61%,
    rgb(25, 23, 36) 17.21%,
    rgb(22, 21, 32) 25.82%,
    rgb(20, 19, 28) 34.42%,
    rgb(18, 18, 24) 43.03%,
    rgb(17, 17, 23) 51.63%
  );
}

/* === MASKS === */

.entrance-message {
  -webkit-mask-image: radial-gradient(circle at 50% 100vh, black 0%, transparent 0%);
  mask-image: radial-gradient(circle at 50% 100vh, black 0%, transparent 0%);
  mask-repeat: no-repeat;
  mask-size: 100% 100%;
}

.mask-wrapper {
  mask-image: url("/images/big-hero-text.svg");
  mask-repeat: no-repeat;
}

/* === ANIMATED GRADIENT BG === */

.animated-gradient-bg {
  background: linear-gradient(135deg, #1e2a52 0%, #6e4b91 100%);
}

/* === RADIAL CONTENT BG === */

.jason-content,
.lucia-life-content {
  background: radial-gradient(ellipse, #111117 20%, transparent 70%);
}
